- [Optimistic create window-scoping](optimistic-create-window-scoping.md) — Add-Entry optimistic insert must target only windows whose date range contains the entry; offline-queued entries never reconcile, so insert-everywhere spreads to every day.
- [Railway prod backend](railway-prod-backend.md) — live backend is on Railway, not Replit; Replit Postgres is a stale backup, so prod data loss isn't restorable from here. Verify via the live Railway URL + demo account.
- [eas update long-running](eas-update-long-running.md) — `eas update` exceeds the ~120s tool limit; backgrounding gets killed (empty log), `pgrep` false-matches the polling shell. Run foreground, then confirm with `eas update:list`. Also: main agent CANNOT publish when tree is dirty (git `.git/index.lock` writes blocked) — delegate to a Project Task or have the user run it.
- [Offline queue synthetic-id identity](offline-queue-synthetic-id-identity.md) — queued entries need stable client-side identity (unique monotonic `queuedAt` → `-id`, `clientId`), serialized AsyncStorage RMW, single-drain guard, and per-item delete re-check so they aren't duplicated/mis-deleted/resurrected.
- [React Query focus refetch in RN](react-query-focus-refetch-rn.md) — Expo app must wire focusManager to AppState or data only refreshes on cold restart ("changes only show after exit+reopen").
