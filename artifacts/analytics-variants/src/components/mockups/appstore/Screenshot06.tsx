import React from "react";
import { AppStoreFrame } from "./_PhoneFrame";
export function Screenshot06() {
  return <AppStoreFrame line1="Hit your" line2="income goals" appImage="/images/app-goals.png" altText="Goals — daily target reached, 100%, entries list" />;
}
